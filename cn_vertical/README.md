# cn_vertical - 中文竖排排版引擎

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![LuaTeX](https://img.shields.io/badge/LuaTeX-required-green.svg)](https://www.luatex.org/)

一个专为 LuaTeX 设计的传统中文竖排排版系统，采用**基于网格的坐标映射**方案，实现精准、灵活的古籍排版效果。

## ✨ 主要特性

- 📜 **传统竖排布局**：从上到下书写，列从右到左排列
- 🎯 **精确网格定位**：基于网格的坐标系统，确保字符对齐一致
- 🎨 **版心装饰**：支持传统版心、鱼尾等古籍装饰元素
- 📐 **灵活的边框系统**：列边框、外边框，可自定义颜色和厚度
- 🔲 **内嵌文本框**：GridTextbox 支持多列布局和对齐控制
- 🎭 **模板系统**：预设风格模板（如四库全书风格）
- 🐛 **调试模式**：可视化网格和盒子边界，便于开发调试

---

## 📦 快速开始

### 基本用法

```latex
\documentclass{guji}

\gujiSetup{
  book-name={四庫全書},
  chapter-title={史記\\卷一},
}

\begin{document}

\begin{guji-content}
天下熙熙，皆为利来；天下攘攘，皆为利往。
\end{guji-content}

\end{document}
```

### 使用模板

```latex
\documentclass{guji}

% 加载四库全书风格模板
\gujiSetup{template=sikuquanshu}

% 设置书名和章节
\setBookName{史記}
\setChapterTitle{卷一·五帝本紀第一}

\begin{document}
\begin{guji-content}
黃帝者，少典之子，姓公孫，名曰軒轅。
\end{guji-content}
\end{document}
```

### GridTextbox 示例

```latex
% 创建一个 3 格高、2 列的文本框
\GridTextbox[height=3, n-cols=2]{
  第一列内容
  第二列内容
}

% 带对齐的文本框
\GridTextbox[
  height=5,
  n-cols=2,
  column-align={left,right},
  box-align=center
]{内容}
```

---

## 🏗️ 架构设计

### 三阶段流水线

本系统采用**三阶段流水线**处理排版任务：

```
┌─────────────────────────────────────────────┐
│  TeX 层 (cn_vertical.sty / guji.cls)       │
│  • 接收用户输入                              │
│  • 管理属性和参数                            │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  协调层 (core_main.lua)                     │
│  • 协调三个处理阶段                          │
│  • 管理多页渲染                              │
└──────────────────┬──────────────────────────┘
                   │
    ┌──────────────┴──────────────┐
    │                             │
    ▼                             ▼
┌─────────────┐    ┌──────────────────────────┐
│ 第一阶段     │    │ Textbox 处理              │
│ flatten_nodes│    │ (core_textbox.lua)       │
│ • 展平嵌套    │    │ • 处理 GridTextbox       │
│ • 提取缩进    │    │ • 多列布局               │
└──────┬───────┘    └──────────────────────────┘
       │
       ▼
┌─────────────┐
│ 第二阶段     │
│ layout_grid │
│ • 虚拟网格   │
│ • 计算坐标   │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────┐
│ 第三阶段 (render_page.lua)      │
│ • 应用坐标                       │
│ • 绘制装饰元素                   │
│   ├─ render_position   (文字)   │
│   ├─ render_border     (边框)   │
│   ├─ render_banxin     (版心)   │
│   ├─ render_background (背景)   │
│   └─ render_yuwei      (鱼尾)   │
└─────────────────────────────────┘
       │
       ▼
    PDF 输出
```

### 模块组织

所有模块按层级组织，使用前缀命名：

| 层级 | 前缀 | 文件示例 | 职责 |
|------|------|----------|------|
| 基础层 | `base_` | `base_constants.lua` | 常量定义、工具函数 |
| 协调层 | `core_` | `core_main.lua` | 流程协调、文本框处理 |
| 展平层 | `flatten_` | `flatten_nodes.lua` | 节点展平、缩进处理 |
| 布局层 | `layout_` | `layout_grid.lua` | 网格布局、坐标计算 |
| 渲染层 | `render_` | `render_page.lua` | 坐标应用、视觉渲染 |

---

## 🎨 主要功能

### 1. 版心与鱼尾

版心是古籍中间的分隔列，包含书名、章节名和装饰性鱼尾：

```latex
\gujiSetup{
  book-name={四庫全書},
  chapter-title={史記\\卷一},
  banxin-upper-ratio=0.28,   % 上部区域占比
  banxin-middle-ratio=0.56,  % 中部区域占比
  banxin-lower-ratio=0.16,   % 下部区域占比
  lower-yuwei=true,          % 是否显示下鱼尾
}
```

### 2. 边框系统

支持多层边框：列边框、外边框，可自定义颜色和厚度：

```latex
\gujiSetup{
  border=true,
  border-thickness=0.5pt,
  border-color=black,
  outer-border=true,
  outer-border-thickness=2pt,
}
```

### 3. 对齐控制

支持垂直和水平对齐：

```latex
\gujiSetup{
  vertical-align=center,  % top, center, bottom
}

\GridTextbox[
  box-align=top,          % 框内垂直对齐
  column-align={left,center,right}  % 每列的水平对齐
]{内容}
```

### 4. 颜色系统

支持背景色和字体颜色：

```latex
\gujiSetup{
  background-color={0.98,0.95,0.90},  % 米黄色背景
  font-color=black,
  border-color={0.4,0.2,0.1},         % 深棕色边框
}
```

### 5. 调试模式

开启调试模式可视化网格和盒子：

```latex
\GujiDebugOn  % 全局开启调试

\gujiSetup{debug=true}  % 或者单独设置
```

---

## 📁 文件结构

```
cn_vertical/
├── README.md              # 本文件
├── LEARNING.md           # 开发经验总结
│
├── LaTeX 接口层
│   ├── cn_vertical.sty   # 核心宏包
│   ├── guji.cls          # 古籍文档类
│   └── sikuquanshu.guji  # 四库全书模板
│
├── 基础层 (base_)
│   ├── base_constants.lua  # 常量定义
│   └── base_utils.lua      # 工具函数
│
├── 协调层 (core_)
│   ├── core_main.lua      # 主协调器
│   └── core_textbox.lua   # 文本框处理
│
├── 第一阶段 (flatten_)
│   └── flatten_nodes.lua  # 节点展平
│
├── 第二阶段 (layout_)
│   └── layout_grid.lua    # 网格布局
│
└── 第三阶段 (render_)
    ├── render_page.lua      # 主渲染器
    ├── render_position.lua  # 文字定位
    ├── render_banxin.lua    # 版心绘制
    ├── render_border.lua    # 边框绘制
    ├── render_background.lua # 背景颜色
    └── render_yuwei.lua     # 鱼尾装饰
```

---

## 🔧 配置参数

### 页面设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `paper-width` | 纸张宽度 | `1136pt` |
| `paper-height` | 纸张高度 | `894.6pt` |
| `margin-top/bottom/left/right` | 页边距 | 各不相同 |

### 网格设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `grid-width` | 网格列宽（字距） | `52pt` |
| `grid-height` | 网格行高 | `45pt` |
| `height` | 列的总高度 | `600pt` |
| `n-column` | 每个版心的列数 | `8` |

### 版心设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `book-name` | 书名（显示在上部） | 空 |
| `chapter-title` | 章节标题（中部） | 空 |
| `chapter-title-cols` | 章节标题列数 | `2` |
| `banxin-upper-ratio` | 上部区域比例 | `0.28` |
| `banxin-middle-ratio` | 中部区域比例 | `0.56` |
| `banxin-lower-ratio` | 下部区域比例 | `0.16` |
| `lower-yuwei` | 是否显示下鱼尾 | `true` |

### GridTextbox 参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `height` | 文本框高度（网格数） | `1` |
| `n-cols` | 列数 | `1` |
| `inner-grid-width` | 内部网格宽度 | 自动计算 |
| `inner-grid-height` | 内部网格高度 | 继承外部 |
| `box-align` | 垂直对齐方式 | `top` |
| `column-align` | 各列水平对齐 | 空 |

---

## 🎓 核心概念

### 网格系统

不同于传统 TeX 的基线对齐，本系统使用**虚拟网格**：

- 页面被划分为统一的格子（grid）
- 每个字符占据一个格子
- 通过 `xoffset/yoffset` 精确定位字符在格子中的位置

### 属性驱动

使用 LuaTeX 的属性系统传递信息：

```lua
\setluatexattribute\cnverticalindent{2}      -- 缩进 2 格
\setluatexattribute\cnverticaltextboxwidth{1}  -- 文本框宽度
```

### 三阶段分离

将"数据提取"、"位置模拟"、"实际着色"分为三个独立步骤：

1. **Flatten**：将嵌套的 TeX 盒子展平为一维节点流
2. **Layout**：在虚拟网格上计算每个节点的坐标
3. **Render**：应用坐标并绘制装饰元素

---

## 📚 示例文档

项目包含多个测试文件：

- `test_textbox_final.tex` - GridTextbox 功能演示
- `test_column_align.tex` - 列对齐测试
- `test_lower_yuwei.tex` - 鱼尾参数测试
- `shiji.tex` - 完整的《史记》排版示例

---

## 🐛 常见问题

### 文字消失了？

1. **检查颜色设置**：确保使用归一化的 RGB 值（`0.0-1.0` 或 `0-255`）
2. **检查坐标范围**：开启 `\GujiDebugOn` 查看是否有蓝色/红色框
3. **检查背景色**：背景色可能遮挡文字（应使用浅色背景）
4. **查看日志**：检查是否有"Missing character"错误

### 编译很慢？

- LuaTeX 第一次编译会比较慢，后续编译会快很多
- 复杂的边框和装饰会增加渲染时间
- 调试模式会显著降低速度，生产环境请关闭

### 如何调整字距和行距？

```latex
\gujiSetup{
  grid-width=52pt,   % 控制字距（列间距）
  grid-height=45pt,  % 控制行距（字符间距）
}
```

---

## 🤝 贡献指南

欢迎贡献！在开发前请：

1. 阅读 [LEARNING.md](LEARNING.md) 了解常见陷阱
2. 为新功能创建测试文件
3. 更新相关文档
4. 保持代码风格一致

---

## 📖 术语对照

| 英文 | 中文 | 说明 |
|------|------|------|
| scaled points (sp) | 缩放点 | TeX 内部单位，1pt = 65536sp |
| big points (bp) | 大点 | PDF 单位，1bp ≈ 1/72 inch |
| glyph | 字形节点 | 表示一个字符 |
| banxin | 版心 | 古籍中间的分隔列 |
| yuwei | 鱼尾 | 版心中的装饰符号 |
| RTL | 从右到左 | Right-To-Left |
| flatten | 展平 | 将嵌套结构转为线性 |
| layout_map | 布局映射 | 节点指针到坐标的映射表 |

---

## 📄 许可证

MIT License - 详见 LICENSE 文件

---

## 🔗 相关链接

- [LuaTeX 官网](https://www.luatex.org/)
- [LuaTeX 参考手册](http://www.luatex.org/svn/trunk/manual/luatex.pdf)
- [开发经验总结](LEARNING.md)

---

## 📝 版本历史

- **v0.5.0 (2026-01-13)**: 添加可选下鱼尾参数、完善文档
- **v0.4.0 (2026-01-13)**: 文件重命名，添加层级前缀，完善中文注释
- **v0.3.0 (2026-01-12)**: 模块化重构，拆分为三阶段流水线
- **v0.2.0**: 添加版心、鱼尾支持
- **v0.1.0**: 初始版本，基础竖排功能

---

*最后更新：2026-01-13*
