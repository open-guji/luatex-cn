# 横排模式（CLReq 优先实现）设计与规划

> 配套文档：`docs/CLREQ-GAP-ANALYSIS.md`（差距分析）
> 本文回答：为什么先做横排、横排模式长什么样、怎么与竖排收敛、要花多少工。

---

## 0. 进度总览（截至 2026-08-02）

| 阶段 | 状态 | 遗留 |
|------|------|------|
| H0 共享内核（标点表 / 分配器 / 禁则） | ✅ 完成 | — |
| H1 横排管道（中西间距 / 标点宽度 / 风格预设 / 引号体例） | ✅ 完成 | xeCJK・luatexja 输出人工对照（移入 H6） |
| H2 禁则 + 行内二次分配（挤压 7 级 / 拉伸 2 级 / 行末挤压 / 悬挂 / 均排） | ✅ 完成 | — |
| H3 横排行间标点（专名号 / 书名号甲 / 着重号） | ✅ 完成 | — |
| H4 行间注（拼音标音 / 中外文对照 / 词对齐） | 🟡 主线完成 | 注文悬伸、单字标音的正文字距联动、注音符号（ㄅㄆㄇ）排布 |
| H5 段落与页面级（缩排 / 凸排 / 孤行孤字 / 段末行对齐 / 号数字号） | 🟡 主线完成 | 标题排布：占行、单页起、同行标题、跨栏标题 |
| H6 测试、文档与发布 | 🟡 进行中 | `docs/CLREQ-CONFORMANCE.md`、用户文档 / Wiki、独立 hori 回归套件 |

**当前口径**：横排后端上，clreq 的**标点规则已基本完备**，**行与段落规则除标题层
之外亦已完备**；缺口集中在标题排布、注音符号，以及对外的符合度声明（H6）。
非典型标点中**叹问号叠加已完成**（H8，PR #139：`？？` `！！` `？！` `！？`
为两字宽刚性整体）；示亡号、科技文献句点与儿化小字（U+16FF2/3）未实现。
点号/间隔号的字面分布已改为**度量锚定**（H7 核心，PR #138：
`shared/luatex-cn-punct-anchors.lua`，大陆/台湾 × 横/竖全矩阵）；
`punct-table` 对 open/close 仍仅按大陆口径建模（台式括号居中口径待 H7 收尾）。

**测试现状**：unit test 41/41 通过（含 `test/unit_test/shared/` 6 个模块、
`test/unit_test/hori/` 4 个模块）；`python3 test/clreq_test.py` 95 条度量断言全绿
（`hori.tex` 主用例 + `stress-unbreakable.tex` 压力用例含叹问号叠加 +
`hori-taiwan.tex` 台式用例 + `vert-punct.tex` 竖排用例含大陆式偏靠与
cm 缩放解析）；回归用例 `basic/hori.tex`、`basic/hori-taiwan.tex`、
`basic/hori-para.tex`；实战示例 `示例/论辩的魂灵`。

---

## 1. 为什么「先横排」是对的

差距分析的初版曾把横排排在最后（P6，「不建议」），理由是与现有横排生态重复。
这个判断漏了三件事，本文修正后已回写差距分析（其 P0–P7 现按「竖排回流」口径描述）。

### 1.1 横排可以白嫖 TeX 的断行器

clreq 的换行禁则本质是「某些位置不许断、某些位置代价高」——这正是 TeX `penalty` 的语义；
可调间隙则正是 `glue` 的语义。横排模式下：

```
禁则       →  penalty（10000 / 可配置代价）
可调空间   →  glue（含 stretch / shrink）
不可分离   →  penalty 10000 包裹
```

**断行位置的搜索完全交给 TeX 的全局最优算法**，我们只负责在
`pre_linebreak_filter` 里把 glue/penalty 插对。而竖排引擎里这套东西全部要自己写
（现在 `layout-grid.lua` 的 kinsoku 是逐字符贪心的，做不到全局最优）。

同样一条 clreq 条款，横排的实现成本大约是竖排的 **1/3**。

### 1.2 真正该共享的东西，横排能先把它验证出来

我原来说「地基是弹性网格」，更准确的说法是：**地基是一个纯函数**——

> 给定：一行/一列的目标长度 L、一串带优先级的可调间隙 `{ideal, min, max, priority}`
> 求：每个间隙的最终值（按 clreq 的挤压 7 级 / 拉伸 2 级顺序依次用尽）

这个函数**与横排/竖排无关，与 TeX 无关**，是纯 Lua、纯数据、零依赖、极易单元测试的东西。
横排先用它（在 `post_linebreak_filter` 里对每一行做二次分配），竖排后用它
（在 `flush_buffer` 里对每一列做分配）。规则层（标点表、类别、优先级、禁则级别）同样共享。

所以「横排先行」不是绕路，而是**在成本最低的场地把共享内核跑通**。

### 1.3 生态里确实有空白

- `xeCJK` 的 `punctstyle` 只做标点压缩的粗粒度风格，没有 clreq 的优先级顺序、四级禁则、行尾悬挂。
- `luatexja` 完备但遵循的是日文 JLREQ，标点字面分布、引号体例、行间标点都按日文规范。
- 行间标点（专名号 / 书名号甲式 / 着重号）、汉语拼音行间注，在横排 LaTeX 生态里基本没有可用实现。

一个真正按 clreq 做的横排包，不是重复造轮子。

### 1.4 「无历史包袱」的额外价值

竖排引擎现在有 grid / natural 双轨、经验偏移量、无条件挤压等既有行为，
任何规范化改动都要担心古籍示例的基线变化（差距分析 R5）。横排是空地：
可以直接按 clreq 的模型建，建完之后**以它为准**去改造竖排，而不是反过来打补丁。

---

## 2. 形态：包优先，类其次

```
tex/
├── shared/                          # 新增：横竖排共享内核（纯 Lua，无 TeX 依赖）
│   ├── luatex-cn-punct-table.lua    # clreq 附录标点全表（单一数据源）
│   ├── luatex-cn-adjust.lua         # 一维优先级空间分配器（上文的"纯函数"）
│   └── luatex-cn-kinsoku.lua        # 禁则规则：四级 + 符号分离禁则
├── hori/                            # 新增：横排后端
│   ├── luatex-cn-hori.sty           # 主包（\usepackage 即可用）
│   ├── luatex-cn-hori-pipeline.lua  # pre/post_linebreak_filter 节点管道
│   ├── luatex-cn-hori-spacing.lua   # 中西间距、标点宽度调整
│   ├── luatex-cn-hori-linemark.lua  # 横排行间标点（单面装）
│   └── luatex-cn-hori-ruby.lua      # 拼音/注音行间注
└── ltc-cn-hbook.cls                 # 薄文档类：横排现代中文书籍
```

**为什么包优先**：`\usepackage{luatex-cn-hori}` 能被任何现有 LaTeX 文档采用，
获取用户与真实反馈的成本远低于「换一个文档类」。文档类只是把包 + 版心参数打包。

**与 luatexja / ctex 的关系**：`xeCJK` 只能运行在 XeTeX 上，与本包（LuaTeX）**根本不可能同时载入**，
不存在冲突问题。真正会打架的是 `luatexja`（同样在 LuaTeX 的断行回调里做标点压缩与禁则），
以及 `ctex`——它在 LuaLaTeX 下的后端就是 luatexja。策略是**检测并互斥**：
载入时若发现 `luatexja` 已加载，发出明确 error/warning 要求二选一；
文档里写清「本包与 luatexja/ctex（LuaLaTeX 模式）的 CJK 断行处理互斥」。不做兼容层——那是无底洞。

---

## 3. 节点管道设计

```
TeX 段落
   │
   ├─ pre_linebreak_filter ──────────────────────────────────┐
   │    1. 分类打标：查 punct-table，写 ATTR_PUNCT_CLASS      │
   │    2. 中西间距：CJK↔Latin 边界插入 glue                  │
   │         ideal 1/4 em, shrink→1/8 em, stretch→1/2 em      │
   │         例外（clreq）：行首/行尾不加；中文点号前后、      │
   │         开始夹注号之后、结束夹注号之前不加                │
   │    3. 标点宽度：按字面分布插 kern，可调部分插 glue        │
   │    4. 禁则：按级别插 penalty（行首禁 / 行尾禁）           │
   │    5. 不可分离：两字宽标点、数字串、数字+单位、货币       │
   │         用 penalty 10000 包裹                            │
   │    6. 每个 glue 上挂优先级属性（ATTR_ADJUST_PRIORITY）    │
   └──────────────────────────────────────────────────────────┘
   │
   ├─ TeX 断行（全局最优，免费）
   │
   ├─ post_linebreak_filter ─────────────────────────────────┐
   │    7. 逐行调用 shared/adjust.lua：按 clreq 优先级顺序     │
   │       重新分配余量（TeX 的比例分配 → clreq 的顺序分配）   │
   │    8. 行首/行尾标点挤压                                   │
   │    9. 行尾点号悬挂（横排默认关闭，可开）                  │
   │   10. 均排 / 单行对齐                                     │
   └──────────────────────────────────────────────────────────┘
   │
   └─ 行间标点 / 行间注：post_linebreak 后按行绘制
```

**第 7 步是关键**：TeX 断行后会按 glue 的 stretch/shrink **比例**同时伸缩全行，
而 clreq 要求**按优先级依次用尽**（先挤行末标点，再挤西文词距，再挤间隔号……）。
所以必须在 `post_linebreak_filter` 里把 TeX 的比例分配结果**覆盖掉**，改用共享分配器的结果。
这一步 `luatexja` 也是类似做法，路径成熟。

---

## 4. 路线图

### H0 · 共享内核（横竖排都要用）✅ 已完成

- ✅ `punct-table.lua`：clreq 附录全表数据化——字符 → {类别, 字面分布(大陆/港台), 宽度,
  可调空间(始/末/两侧), 不可分离, 直排右旋, 行首禁则, 行尾禁则}。
- ✅ `adjust.lua`：一维优先级空间分配器。输入 `{目标长度, 间隙列表}`，输出每个间隙的最终值。
  实现 clreq 挤压 7 级 + 拉伸 2 级 + 兜底均分，含各项上下限
  （7 级：行末标点 → 西文词距 → 间隔号 → 夹注符号 → 逗号/顿号/分号 → 中西间距 → 句号/问号/叹号）。
- ✅ `kinsoku.lua`：四级禁则（不处理 / 基本 / GB / 严格）+ 符号分离禁则，
  输出「penalty 值」（横排后端用）或「推出/挤进决策」（竖排后端用）。
- ✅ 全部是纯 Lua 纯函数，用 `texlua` 直接单测，不需要编译 PDF。

**验收**：三个模块单测覆盖率 > 90%；clreq 条款与测试用例一一对应。
**工作量：12–18 人日**

> **完成记录（2026-07-31，PR #125）**：交付 `tex/shared/` 三模块 +
> `ai_must_read/clreq-shared-core.md` 接口契约 + `test/unit_test/shared/`
> 62 个用例（每条标注 clreq 条款，覆盖全部挤压/拉伸优先级分支与四级禁则）。
> unit test 33/33、回归三套全绿。实现注记：① 附录「不可分离/直排右旋」两列
> 从 clreq 原文逐条核对；② clreq 基本级行首禁含连接号/间隔号/分隔号，
> 严于现行竖排引擎，P2 接线时按 R5 分档启用；③ 长串两字宽标点的对边界
> 放行由 `pair_boundary_breakable()` 提供，调用方持 run 信息时使用。

### H1 · 横排节点管道（骨架 + 中西间距 + 标点宽度）✅ 已完成

- ✅ `pre_linebreak_filter` 全流程；中西间距 glue；标点字面分布与宽度调整。
- ✅ 大陆式 / 港台式 / 不调整 三套预设（台式专属回归/断言用例 `hori-taiwan.tex`，TW-Kai）。
- ✅ 引号体例转换（**默认 keep 不改来稿**；`quote-style=auto/curly/corner`
  显式开启——传统引号↔弯引号一一映射，嵌套深度与开闭角色保持，
  大陆先双后单 / 台湾先单后双随之成立）。
- ✅ 连续标点缩减、行首夹注符号缩减、西文词距界限（PR #131 的符合性核查修复）。
- ⬜ 与 xeCJK（XeLaTeX 单独编译）/ luatexja 输出做人工对照 → **移入 H6**
  （属对外发布前的横向比对，不阻塞管道本身）。

**验收**：✅ 横排断言测试（PDF 内容流解析）验证字距度量。
**工作量：15–22 人日**

> **完成记录（2026-07-31 至 08-01，PR #127 / #128）**：
> - PR #127：`tex/hori/` 三文件——spacing 纯函数决策（CJK 断点、中西间距
>   1/4em 含三类 clreq 例外、标点空白侧 shrink 挂 `ATTR_ADJUST_CLASS`）、
>   pipeline 节点插入、主包（style/kinsoku-level/paragraph-indent 键值，
>   luatexja 互斥检测）。单测 34 个；回归用例 `basic/hori.tex`（思源宋体
>   SC，须 `Script=CJK, Language={Chinese Simplified}` 锁定 hani/ZHS，
>   否则 latn/dflt 的 locl 会把省略号换成贴基线西文形）。
> - PR #128：clreq 断言测试框架 `test/clreq_test.py`（本属 P0/验收项，
>   提前建成）——53 条度量断言全绿，含压力用例（token 相位扫描普查 +
>   负对照验证：关闭禁则后全角数字串确实拆行，断言具区分力）。
> - 实现注记：① 半角 token（95% 等）的不可拆由「西文边界不插断点」
>   构造性保证，kinsoku penalty 实际保护的是全角数字与 CJK 邻接边界；
>   ② 思源宋体 liga 把 —— 合成单个 U+2E3A 字形（字幅实测 2.000em）；
>   ③ 行末标点挤压现状 0/10 行 ≤ 半字宽（TeX 断点丢弃 glue 所致，
>   优先级倒挂），是 H2 的首要量化目标。
> - **H1 收尾（后续 PR 完成）**：引号体例转换（应用户要求默认 keep、
>   显式开启）与 `hori-taiwan.tex` 台式用例（引号转换、？！固定一字宽、
>   台式行末 0.75em 占位共 5 条断言）。仅余 xeCJK/luatexja 人工对照。

### H2 · 断行禁则 + 行内调整二次分配 ✅ 已完成

- ✅ 禁则 penalty 注入；不可分离单元包裹（已随 H1 的 PR #127 完成）。
- ✅ `post_linebreak_filter` 接入 `adjust.lua`，覆盖 TeX 的比例分配。
- ✅ 行尾标点挤压（量化目标达成：断言框架的「行末标点 ≤ 半字宽」
  从 0/10 行升到全部受挤压行，且负对照证明断言具区分力）。
- ✅ 均排（行级拉伸兜底均分由分配器完成；`\均排[宽度]{文字}` 命令用于
  标题/表格单元格的整块均排）。
- ✅ 行尾点号悬挂（`hanging-punct=true` 显式开启，默认关——港台式点号
  居中不宜悬挂；开启后行末点号整字悬于版口外，实测 x0 ≈ 右缘）。
- ✅ 单行对齐四种（居中/齐头/齐尾/均排）：前三种由 LaTeX 行盒对齐提供，
  均排由 `\均排[宽度]{}` 提供；段末行的四种对齐随 H5 的 `last-line` 键落地。

**验收**：✅ clreq 挤压/拉伸优先级断言全绿（含台式 0.75em 行末占位断言）。
**工作量：10–16 人日**

> **完成记录（2026-07-31）**：`hori/luatex-cn-hori-adjust-line.lua`——
> 纯函数 `plan()`（可 texlua 单测）+ 节点应用层。每行收集带
> `ATTR_ADJUST_CLASS` 的 glue（含 pre 阶段新打标的西文词距）与行末标点
> 字形内空白，交 `shared/adjust.lua` 按 clreq 7 级挤压 / 2 级拉伸重解，
> 覆写 glue 宽度后重排该行。行末空白用负 kern 回收（断点 glue 被 TeX
> 丢弃后字形空白是唯一遗留），`line-end-punct=compress`（默认，恒定半字
> 宽）/`natural`（仅按行紧度回收）。含无穷 glue 的行（段末
> parfillskip、`\hfill`）整行跳过（HR1）。配套规则：符号分离禁则单元
> 内部的 glue 去 stretch 去 class——数字串既禁拆也禁被兜底均分撑开。
> 收尾 PR 补：行尾点号悬挂（`line_end_blank_em` 悬挂分支返回整字宽，
> 复用同一负 kern 通道）与 `\均排` 命令。

### H3 · 横排行间标点（单面装）✅ 已完成

- ✅ 专名号（下划线）、书名号甲式（波浪线）、着重号，均在文字**下方**。
- ✅ 「先线后点」原则：行间线紧贴汉字（-0.18em），着重号在其下（-0.30em）。
- ✅ 相邻标注两侧缩短（各 1/16 em，合 1/8 em）；标点上不加着重号；
  着重号逐字与字心对齐（疏排自然保持）。
- ✅ 行距下限校验：单面装 ≥ 0.5 em，不足时 warning。

**验收**：✅ 单测（run 切分/交界缩短/路径生成）+ 回归基线；波浪线复用
竖排 `linemark.lua` 的正弦四段贝塞尔构造（C1 连续，轴向换横）。
**工作量：8–12 人日**

> **完成记录（2026-07-31）**：`hori/luatex-cn-hori-linemark.lua`——
> `\专名{}`/`\书名号甲{}`（别名 `\书名甲`）/`\着重{}` 通过组内
> `tex.setattribute`（组局部）打属性，属性值 = 种类 + 4×流水号，
> 相邻同类标注因流水号不同而分 run。post_linebreak 在 H2 调整后逐行
> 重建 x 坐标（`effective_glue`），按 run 插 origin-mode pdf literal。
> 跨行标注每行独立成段，天然正确。

### H4 · 行间注（横排是主场）🟡 大部分完成

- ✅ 汉语拼音分词连写标音（`\拼音{基文}{zhù wén}`，音节按空格切分）。
- ✅ 词对齐：注文短于基文 → 加大注文字距（n 槽位、两端半槽，单元素退化
  为居中）；长于基文 → 加大基文字距。求解在 `shared/luatex-cn-ruby-metrics.lua`（HR5）。
- ✅ 中外文对照（`\对照{}{}`，同一机制）。
- ✅ 相邻注文最小间距 ≥ 1/4 注音字宽——无悬伸布局下构造性满足。
- ⬜ 注文悬伸到相邻基字上方（需跨块协商，悬伸实现须显式校验最小间距）。
- ⬜ 单字标音的「正文加大字距」联动（当前每个注块独立，不扩邻字字距）。
- ⬜ 注音符号横排（注音在汉字右侧、3:10 比例）——度量常量已入共享层，
  排布待竖排注音（P4）一并实现。

**验收**：✅ 共享常量（3:10、1:15 轻声、1/4 最小间距）单测；
✅ PDF 断言（注文行存在、注文块与基文块中心对齐 ≤ 0.1em）。
**工作量：15–25 人日**

> **完成记录（2026-07-31）**：度量与词对齐规则在
> `shared/luatex-cn-ruby-metrics.lua`（纯 Lua，`tex_layout()` 经
> `token.set_macro` 回填 TeX）；排布在 `luatex-cn-hori.sty`（expl3，
> 测量与重建用同一构造以保证宽度严格一致）。注块两侧补零宽 glue 恢复
> 断行点。`ruby-size`/`ruby-gap` 键默认值取自共享层。

### H5 · 段落与页面级 🟡 大部分完成

- ✅ 段首缩排（H1 已有 `paragraph-indent`）、凸排（`凸排` 环境：首行顶格、
  次行起缩排，默认 2em）、段落缩排（`段落缩排` 环境：整段左/右缩）。
- ✅ 孤行孤字：段末孤字避免（`orphan-char` 默认开——段末内容字前断点补
  penalty 10000，TeX 全局断行自动「前行借字」，孤字可带后随标点）；
  页首/页尾孤行（`widow-penalty`/`club-penalty` 键，默认 10000 禁止）。
- ✅ 段末行对齐四种（原 H2 遗留归并）：`last-line = left(默认) | center |
  right | justify`——center/right 平移行盒（parfillskip 伸展量即空量）；
  justify 清零 parfillskip、空量作 surplus 交 H2 分配器按优先级吸收。
  **均排口径（clreq）**：均排是行级机制——没排满的行（多因禁则）按
  拉伸优先级 + 兜底均分铺满使两端对齐，**正文段落末行不均排**；这两点
  默认行为均已符合（中间行由 H2 分配器铺满、末行 left）。`justify` 对应
  clreq 的显式例外体例：列表、人名/物品名单、表格标题求体例一致时
  「段落末行或仅有一行亦使用均排」。
- ✅ 号数字号（`\字号{五号}` / `\字号{小四}[1.7]`）：号数表在
  `shared/luatex-cn-type-sizes.lua`（bp 制，初号…八号含小半档），
  横竖排共享。
- ⬜ 标题：占行（整数倍行高）、单页起（右页起）、同行标题（一个全角
  空白间隔）、跨栏标题——涉及 sectioning 集成，另行设计。

**验收**：✅ 单测（孤字保护/surplus 分配/号数表）+ 回归用例
`hori-para.tex`（版面渲染各段配置说明）。
**工作量：10–15 人日**

> **完成记录（2026-07-31）**：孤字保护在 pre_linebreak 回调尾部做反向
> 扫描（跳过段尾标点串定位「内容字」，仅汉字触发；已有 ≥10000 penalty
> 或无前字可借则不动）。默认开启使既有 hori.tex 两处孤字段落
> （「格。」「排。」）自动借字重排，基线随之更新。段末行 justify 会把
> 说明行等短段落轻微拉伸——语义使然，文档中按需局部切换。

### H6 · 测试、文档与发布 🟡 进行中

- ✅ clreq 断言用例：`test/clreq_test.py` 67 条度量断言全绿，覆盖主用例、
  压力用例（token 相位扫描 + 负对照）与台式用例；已随 unit test / regression
  一并进 CI。
- ✅ 示例文档：`示例/论辩的魂灵`（鲁迅，横排 clreq 管道实战，版面上标注启用的特性）。
- 🟡 横排回归用例现挂在 `basic` 套件下（`hori.tex` / `hori-taiwan.tex` /
  `hori-para.tex`）；是否独立成 `test/regression_test/hori/` 套件待定——
  用例增多后再拆，现在拆只增加基线维护面。
- ⬜ `docs/CLREQ-CONFORMANCE.md`：符合度声明矩阵尚未建立。这是对外宣称
  「符合 clreq（横排）」的前置条件，也是 H6 的主要剩余工作。
- ⬜ 用户文档 / Wiki：`luatex-cn-hori` 的键值表（style / kinsoku-level /
  line-end-punct / hanging-punct / adjacent-punct / line-start-bracket /
  quote-style / orphan-char / last-line / widow-penalty / club-penalty 等）
  与命令表（`\专名` `\书名号甲` `\着重` `\拼音` `\对照` `\均排` `\字号`
  `凸排` `段落缩排`）未成篇。
- ⬜ 与 xeCJK / luatexja 的输出人工对照（自 H1 移入）。

**工作量：10–14 人日（已消耗约 4）**

### H7 · 标点字面重定位（punct-reposition）🟡 核心已落地

> **进度（2026-08-02，PR #136 审阅图 → PR #138 实现）**：点号/间隔号类的
> 墨心锚定已交付为 `shared/luatex-cn-punct-anchors.lua`（style × mode
> 全矩阵锚点，样板字体实测值；接口见 `ai_must_read/clreq-shared-core.md`
> §1ter）。后端接线：竖排 `punct.render`（大陆式恒开；台式仅
> `squeeze-mode=context` 挡位，保护 ltc-guji 既有版面）、横排
> `hori-pipeline.apply_ink_anchor`。实现走的是「绝对锚点」而非下文的
> 「探测 + 相对偏移」——锚点不能从 Tm 相对坐标量（xoffset 会写进 Tm）。
> **剩余**：台式括号（open/close）的 `space=both` 居中口径与其重定位，
> 即下文「适用范围」第 2 条；`punct-reposition = auto` 的字体自动探测未做
> （现按 style 无条件锚定 + 0.002em 死区）。

**原设计案（供剩余部分参考）**：

**动机**：一款字体的标点字面通常只固化一种习惯——大陆式（墨迹靠侧、
空白可压缩）或台式（居中占一格，设计进字体）。用户配置的 `style` 与
字体原生习惯不符时（如用 TW-Kai 排大陆式），标点位置就是错的。
利用字形特性数据把墨迹**移动**到目标位置，可让任意字体服从任意 style。

**适用范围**（两式差异是「位置」而非「形状」的都适用）：

- **点号** 。，、；：？！——大陆式墨迹在字框左下、空白在末端；
  台式垂直水平均居中。移动 = `xoffset`（大陆↔台式约 ±0.25em）+
  `yoffset`（台式点号垂直居中）。
- **括号 / 书名号 / 夹注号** （）《》〈〉【】〔〕等——**同样有风格
  差异**：大陆习惯字形靠两侧（开括号墨迹贴右、闭括号贴左，靠文字一侧
  无空白、外侧空白可压缩）；台湾习惯居中占一格，空白分居两侧，
  设计进字体。移动方向与点号同理。
  ⚠ 现行 `punct-table` 对 open/close 类按大陆口径建模
  （`space=start/end`、shrink 0.5）——做本项时须把台式括号改为
  `space=both` 的居中口径，挤压/行末回收随之分侧。
- **间隔号**：GB 半宽 vs 台式 1em 是 advance 差异，用 kern 而非
  offset，单独处理。

**不适用**：引号——台式弯引号常为西文比例字形、「」是另一码位，
属字形差异，由已实现的 `quote-style` 转换负责。

**实现要点**：

1. 探测：luaotfload `descriptions[gid].boundingbox` 取「。」「（」的
   墨迹中心（font units → em），中心 x ≈ 0.25em → 大陆式字面、
   ≈ 0.5em → 台式；每字体测一次并缓存。
2. 优先用字体自带机制：带 `locl` 的字体（思源系）用
   `Language={Chinese Simplified/Traditional}` 切换，探测到已匹配则不动；
   offset 仅为「单习惯字体」（TW-Kai 等）兜底。
3. 施加：`pre_linebreak_filter` 的既有逐字扫描里设 glyph
   `xoffset`/`yoffset`（只移墨迹、不改 advance）。挤压/禁则/行末回收
   本就按配置 style 查共享表——移动使字体实际字面与该假设一致，
   规则层零改动。
4. 键：`punct-reposition = auto | off`（默认 off，不影响现有输出）。
5. 验收：像素回归 + 断言框架增加「墨迹位置」维度（内容流绘制位置
   对照 boundingbox）。

**工作量：3–5 人日**（核心 2–3 + 台式括号口径修正与测试 1–2）

---

### H8 · 叹问号叠加 ✅ 已完成

> **完成记录（2026-08-02，PR #139，作为 P2 前置项交付）**：按下述方案
> 实施，三处配合缺一不可（详见 `ai_must_read/clreq-shared-core.md` §1.3）：
> ① `punct-table.is_stacked_pair(a, b)` 放行异字组合（两侧同属
> question/exclamation），`is_unbreakable_pair` 一并放行；
> ② `kinsoku.no_break_between` 把两字宽单元判定移到行首禁则**之前**——
> 两符号本身都是行首禁则字符，否则 reason 被报成 `forbid_start`，
> 后端 RIGID 集合不认；③ hori 刚性单元内部除 stretch/class 外连
> **shrink** 一并清零——？！是点号、字面自带可挤空白，与本无 shrink 的
> —— 不同，不清零则挤压行仍会压到 2 字宽以下。验收：`clreq_test.py`
> 增 5 条断言（同行相邻、内部零间隙、总占位 ≈2em、挤压行不变形 +
> 负对照），压力用例 `stress-unbreakable.tex` 扩充；unit test 覆盖
> punct-table / kinsoku / hori-spacing 三层。竖排宽度侧待 P2 接线后生效。

原设计案（已按此实施）：

clreq 的「叹问号叠加」是把连排的问号/叹号当作**一个两字幅的整体**，
不只是 `？？` `！！` 这种同字重复——**`？！` `！？` 这类异字组合同样适用**
（`？？？` 之类三连亦然）。处理口径应与 `——` `……` 完全一致：

- 两字幅整体，**内部不挤压**（现在会被 H2 分配器按 `fullstop_group` 压掉半字）；
- 不拆行（现在靠两符号都属行首禁则侥幸成立，不是显式规则）；
- 不被兜底均分撑开（同 RIGID 单元的 stretch 清零与去 class）。

改动点（都在共享层，后端零改动，符合 HR5）：

1. `punct-table.is_unbreakable_pair(a, b)`：放宽 `a == b` 的限制——
   两侧同属 `question`/`exclamation` 即为叠加对；dash/ellipsis 维持原判定。
2. `kinsoku.no_break_between`：为叠加对返回专门的 reason（如 `stacked_mark`），
   后端把它并入 `RIGID_REASONS`，走与破折号/省略号同一条刚性通道。
3. 预组合码位 `‼`(U+203C) `⁇`(U+2047) 已在表内，保持不变；三连以上按
   `pair_boundary_breakable()` 的既有 run 逻辑处理。
4. 验收：`clreq_test.py` 增断言——`？！` 同行相邻、内部间隙 ≈ 0、
   总占位 ≈ 2em；并加负对照（关闭后确实被压缩）。

**工作量：1–2 人日**

---

## 5. 收敛：横排成果如何回流竖排

| 共享物 | 横排产出 | 竖排如何接 |
|--------|---------|-----------|
| `punct-table.lua` | H0 建成并被横排验证 | 竖排 `punct.lua` 的 `CL_*` / `VERT_FORM_MAP` 改为从表派生 |
| `adjust.lua` | H0 建成，H2 在行级用 | 竖排 `flush_buffer` 在列级调用同一函数——**这就是原 P2 的核心** |
| `kinsoku.lua` | H0 建成，H2 输出 penalty | 竖排输出「挤进/推出」决策，替换现有贪心逻辑 |
| 注音度量常量 | H4 落地 | 竖排注音（原 P4）直接复用，只改摆放方向 |
| clreq 断言测试框架 | H6 建成 | 竖排用例照抄结构，换文档类 |

回流之后，差距分析里的原估算可下调：

| 原阶段 | 原估算 | 复用共享内核后 |
|--------|--------|---------------|
| P1 标点系统达标 | 22 人日 | **14 人日** |
| P2 弹性行内调整引擎 | 32 人日 | **20 人日**（求解器已建成并验证，只剩接线与双轨合并）|
| P4 行间注 · 注音 | 25 人日 | **16 人日** |

---

## 6. 工作量总账

| 阶段 | 乐观 | 期望 | 悲观 | 状态 | 剩余（期望） |
|------|------|------|------|------|------|
| H0 共享内核 | 12 | 15 | 18 | ✅ | 0 |
| H1 横排管道 | 15 | 18 | 22 | ✅ | 0 |
| H2 禁则 + 行内调整 | 10 | 13 | 16 | ✅ | 0 |
| H3 横排行间标点 | 8 | 10 | 12 | ✅ | 0 |
| H4 行间注 | 15 | 20 | 25 | 🟡 | 8（注音符号排布 + 悬伸/字距联动） |
| H5 段落与页面 | 10 | 12 | 15 | 🟡 | 5（标题排布） |
| H6 测试文档发布 | 10 | 12 | 14 | 🟡 | 8（符合度矩阵 + 用户文档 + 对照） |
| **横排小计** | **80** | **100** | **122** | | **约 21** |

即：横排线已完成约 **80%**，剩余约 21 人日集中在注音符号、标题排布与对外文档；
H0–H3 的规则内核部分已闭环。

### 与原方案对比

| 方案 | 交付物 | 期望工作量 |
|------|--------|-----------|
| 原方案（只做竖排，口径 A） | 竖排 clreq 子集 | 132 人日 |
| **新方案：横排先行 + 竖排回流** | **横排 clreq 实现 + 竖排 clreq 子集** | **100 + 约 93 = 193 人日** |

> 竖排 93 = P1 14 + P2 20 + P3 18 + P4 16 + P5 15 + P7 10（第 5 节回流折扣后；P0 已被 H0 吸收）。

多花约 **61 人日（+46%）**，换来**多一个完整的横排产品**、
**一套被真实验证过的共享内核**、以及竖排改造风险的显著下降。
如果只算「拿到第一个可对外宣称的 clreq 实现」这个里程碑：
原方案要 45–60 人日（竖排 M1），新方案 H0+H1+H2 = **约 46 人日**，两者相当——
但新方案的产出更干净，且不动古籍基线。

---

## 7. 风险

| # | 风险 | 应对 |
|---|------|------|
| HR1 | `post_linebreak_filter` 里覆盖 TeX 的 glue 分配，可能与 `\hfill`、表格、数学环境冲突 | 只处理带 `ATTR_ADJUST_PRIORITY` 的 glue，其余原样保留；数学/表格内整体跳过 |
| HR2 | 与 luatexja / ctex（LuaLaTeX 后端即 luatexja）同时载入打架；xeCJK 为 XeTeX 专用，不构成冲突 | 载入期检测 luatexja + 明确 error/warning 要求二选一；文档写明互斥 |
| HR3 | 断行代价与禁则代价的调参 | penalty 值全部做成可配置键，提供三套预设，用真实文档做视觉评审 |
| HR4 | H4 拼音注音对字体与断行的影响（注文比基文宽会撑开字距） | 按 clreq「为正文设定较大字距」的做法，把字距预留做成显式参数而非自适应 |
| HR5 | 两条产品线并行导致规则实现发散 | **硬性约束：任何 clreq 规则只允许写在 `shared/`，后端只做编码与绘制** |

---

## 8. 建议的第一步 ✅ 已完成（PR #125）

**H0 单独立项，先出接口设计再写代码**。具体交付：

1. ✅ `ai_must_read/clreq-shared-core.md`：`adjust.lua` 的输入输出契约、
   `punct-table.lua` 的字段定义、`kinsoku.lua` 的决策语义。
2. ✅ `tex/shared/luatex-cn-punct-table.lua` + `texlua` 单测。
3. ✅ `tex/shared/luatex-cn-adjust.lua` + 覆盖 clreq 挤压/拉伸全部优先级分支的单测。

这三样做完（约 15 人日），横排和竖排两条线就都有了地基，之后可以并行推进。
实际交付连同 `kinsoku.lua` 一并完成（H0 全部三模块）。

**此后的实际推进**：H1 → H2 → H3/H4 → H5 均已落地（见第 0 节总览）。
当前建议的下一步有两条，可择一或并行：

1. **H6 的符合度矩阵**（`docs/CLREQ-CONFORMANCE.md`）——成本低、对外价值最高，
   做完即可正式声称「横排符合 clreq 标点与行/段落规则」，并把标题排布、注音符号、
   非典型标点等如实标为「未实现」。
2. **P2 竖排接线**——P1 的宽度调整（#132）与字面锚定（#138）已回流，
   叹问号叠加等 P2 前置（#137/#139）已清；vbook 两类仍缺优先级模型、
   禁则级别硬编码、行尾悬挂与均排缺失，是当前项目主战场与 clreq 现状的
   最大落差。接线设计 `docs/CLREQ-VERTICAL-ADJUST-DESIGN.md` 待评审。
